# bingr — UI Audit: Redundancy, Contradiction & Gap Analysis

**Date:** 3 August 2026
**Companion to:** [`BINGR_AUDIT_REPORT.md`](BINGR_AUDIT_REPORT.md) (functional/security/backend audit — separate document, separate ID scheme, do not confuse `C1`–`C10` there with anything here)
**Scope:** cross-screen design analysis — where the app repeats itself, contradicts itself, or leaves an obvious gap. Not a per-screen review (that's already covered in the earlier audit's §3); this looks *across* screens.
**Status:** tracking document. Goal is 100% closure — every row gets fixed, none skipped. Update the **Status** column as work lands; do not delete rows once resolved, mark them `✅ fixed` so the record stays complete.

**Decisions locked in before this list was written** (from the clarifying-questions round — informs the resolutions below, don't relitigate without reason):
- **Audience:** growth product — designing for a stranger landing cold, not just friends.
- **Visual direction:** bold & maximalist — full creative direction in [`BINGR_DESIGN_SYSTEM.md`](BINGR_DESIGN_SYSTEM.md).
- **Theme priority:** dark-first, light mode a faithful full adaptation, not an afterthought.
- **Scope authority:** resolutions below may include real feature consolidation/cuts, not just visual polish.

---

## How to use this document

Three tables — **Redundancies**, **Contradictions**, **Gaps** — each row identified (`RD#`/`CX#`/`GP#`), severity-rated, with a concrete resolution and a status. When a fix ships, change Status to `✅ fixed` and add the commit/PR reference in a trailing note, matching the pattern in `BINGR_AUDIT_REPORT.md`.

Severity here is **UX severity**, not the security severity scale from the other audit — 🔴 High / 🟠 Medium / 🟡 Low, meaning "how much this actively confuses or costs the user," not "how exploitable."

---

## Table 1 — Redundancies

*Where bingr implements the same idea twice.*

| ID | What | Where | Severity | Resolution | Status |
|---|---|---|---|---|---|
| RD1 | Rankings tab and public-profile "Top Rated" are two hand-built implementations of the same sorted-by-rating list, styled differently (rank number treatment, star display, presence/absence of a type filter). | [Rankings.jsx](src/pages/Rankings.jsx), [UserProfilePage.jsx](src/pages/UserProfilePage.jsx) rankings tab | 🔴 High | Merge into one `<RankedList items showFilter interactive>` component. Own-profile use gets the filter + tap-through; visiting a profile gets a read-only subset. One file, one visual language, two call sites. | ✅ fixed (Phase 2a) — [RankedList.jsx](src/components/RankedList.jsx), both call sites migrated |
| RD2 | Watchlist / Watching / Watched are three nav destinations for what is architecturally already one component (`LibraryTab` parameterized by `status`). The redundancy is in navigation, not code — three tab slots, three page loads, three empty states to maintain for one dataset. | [App.jsx](src/App.jsx) TABS array, [LibraryTab.jsx](src/pages/LibraryTab.jsx) | 🔴 High | Collapse to a single **Library** destination with a segmented status control (All / Watchlist / Watching / Watched) plus the existing Movie/TV filter. Frees two tab slots — directly helps GP-navigation below. | ✅ fixed (Phase 2a) — one `library` tab, status segmented control in [LibraryTab.jsx](src/pages/LibraryTab.jsx) |
| RD3 | Rating has two homes with an undocumented sync rule: `bingr_library.rating` and `bingr_diary.rating` are separate columns. A diary log syncs to the library rating **only on first watch** — a rewatch's rating silently stays diary-only, with no UI explanation of this rule anywhere. | [App.jsx](src/App.jsx) diary-log handler, `useLibrary`/`useDiary` | 🔴 High | Make the library rating always reflect the *most recent* diary rating, rewatch included. When a rewatch rating differs from the current library rating, ask explicitly ("Update your overall rating to X?") rather than applying a silent, undocumented rule. | open |
| RD4 | `ExportPanel` exports the whole library regardless of active tab, yet is re-mounted independently at the top of Watchlist, Watching, and Watched. | [App.jsx](src/App.jsx) fallback tab branch | 🟠 Medium | Resolves automatically once RD2 lands — one Library view, one ExportPanel. If RD2 is deferred, move export to a profile/settings-level action instead. | ✅ fixed (Phase 2a) — resolved automatically by RD2 |
| RD5 | Diary rows and Feed items are two independently-coded card designs for the same underlying fact ("user watched/rated title on date") — different spacing, different rating treatment, different poster size (see CX1). | [DiaryPage.jsx](src/pages/DiaryPage.jsx), [ActivityFeed.jsx](src/pages/ActivityFeed.jsx) `FeedItem` | 🟠 Medium | One `<WatchLogCard variant="diary"|"feed">` component. `feed` variant adds the avatar + `"@user watched"` line; `diary` variant omits it (redundant when it's already "yours"). | ✅ fixed (Phase 3) — [WatchLogCard.jsx](src/components/WatchLogCard.jsx), both files migrated |
| RD6 | Stats computed once (`computeStats()`) but rendered twice by hand: `StatsPage` and the profile's "stats" tab independently reimplement the tile grid, and the profile version drops the Wrapped hero, rating-distribution bars, and top-5 list entirely rather than reusing a reduced composition. | [StatsPage.jsx](src/pages/StatsPage.jsx), [UserProfilePage.jsx](src/pages/UserProfilePage.jsx) stats tab | 🟠 Medium | Extract the stat-tile grid, activity chart, and rating-distribution bars as shared components. `StatsPage` composes all of them plus the owner-only Wrapped hero; the public profile composes a deliberate subset — not a re-implementation. | ✅ fixed — [StatTile.jsx](src/components/stats/StatTile.jsx) + [ActivityChart.jsx](src/components/stats/ActivityChart.jsx) shared; `StatsPage` composes them plus Wrapped hero/rating breakdown/top-5, `UserProfilePage` composes the reduced subset |
| RD7 | Follow/unfollow button hand-implemented twice with separately-typed styling for the identical action. | [FindPeople.jsx](src/components/FindPeople.jsx) `UserRow`, [UserProfilePage.jsx](src/pages/UserProfilePage.jsx) | 🟡 Low | One `<FollowButton isFollowing onToggle loading>` component. | ✅ fixed — [FollowButton.jsx](src/components/ui/FollowButton.jsx) now used in both |
| RD8 | Two search boxes, two silently-different domains: header search (global) searches TMDB titles; FindPeople's search (buried inside the Feed tab) searches bingr usernames. Nothing signals the scope difference — typing a person's name into the header gives "no results" with no hint people-search lives elsewhere. | header search bar, [FindPeople.jsx](src/components/FindPeople.jsx) | 🟠 Medium | Don't unify the search *logic* (genuinely different data) — unify the *entry point*. Either make the header search a combined typeahead with Titles/People sections, or label it explicitly ("Search titles…") so scope is never ambiguous. See GP-nav below for how this interacts with removing FindPeople from Feed. | partial (Phase 2b) — took the lighter-weight option the resolution explicitly permits: header placeholder now reads "Search titles & shows…", and its no-results state links straight to Feed's people search. The combined Titles/People typeahead (the fuller option) is still open, deliberately deferred as new-feature scope rather than nav-chrome scope. |
| RD9 | The rewatch badge (🔁) is independently re-implemented at four render sites with four separately-typed surrounding styles. | [DetailPanel.jsx](src/components/DetailPanel.jsx), [DiaryPage.jsx](src/pages/DiaryPage.jsx), [ActivityFeed.jsx](src/pages/ActivityFeed.jsx), [UserProfilePage.jsx](src/pages/UserProfilePage.jsx) | 🟡 Low | Folds into RD5 — becomes one prop-driven badge element inside `<WatchLogCard>` instead of four copies. | ✅ fixed — DiaryPage/ActivityFeed/UserProfilePage's diary tab all share `<WatchLogCard>`'s one badge. DetailPanel's rewatch element turned out to be a different affordance on inspection (a "log rewatch" action button, not a status badge on a past entry) — not actually in this row's scope, so nothing left there to fold in. |
| RD10 | Privacy Policy / Terms of Service links appeared in three independently hand-typed places at once (header avatar dropdown, You hub's Account tab, and the global footer); Supporters and Delete Account were triplicated the same way. The header dropdown and Account tab were also two separately-typed copies of the identical 8-item action list, free to drift from each other. User-reported. | [AccountMenu.jsx](src/components/nav/AccountMenu.jsx), [YouHub.jsx](src/pages/YouHub.jsx) Account tab, [NavShell.jsx](src/components/nav/NavShell.jsx) footer | 🟠 Medium | Legal links get exactly one home — the footer (standard site-footer convention, already present on every screen). Extract the account-menu's shared items (icon/label/order/danger) into one config, consumed by both the dropdown and the Account tab, so the two "reachable two ways" menus can't silently diverge. | ✅ fixed and browser-verified — new [accountMenuItems.js](src/components/nav/accountMenuItems.js) is the single source of truth for the account menu's contents (Edit profile / View public profile / Admin-or-Feedback / Supporters / Sign out / Delete account), consumed by both `AccountMenu.jsx` and `YouHub.jsx`'s Account tab, each wiring its own click handlers by item id. Privacy Policy and Terms of Service removed from both menus entirely — footer is now their only home. Footer trimmed to legal links + copyright only; Supporters and Delete Account (already reachable via both account menus) no longer duplicated there too. |

---

## Table 2 — Contradictions

*Where the same concept gets different treatment depending on which screen you're looking at.*

| ID | What | Variants found | Severity | Resolution | Status |
|---|---|---|---|---|---|
| CX1 | Poster thumbnails — seven distinct size/radius treatments for three conceptual contexts. | Grid tile: `aspect-ratio 2/3` in a radius-12 card. List-row thumbnail: **four different pairs** — 36×54/r5, 38×56/r5, 42×62/r6, 30×44/r4. Detail hero: 110×165/r10. Provider logo: 18×18/r4. | 🔴 High | Exactly **three poster tokens** — `poster.sm` (list row), `poster.md` (grid tile), `poster.lg` (hero) — full spec in `BINGR_DESIGN_SYSTEM.md`. All seven current variants map onto these three; no new sizes get invented. | ✅ fixed — every poster context in the app renders through [PosterTile.jsx](src/components/ui/PosterTile.jsx) (sm/md/lg). Detail hero doesn't map onto `poster.lg` — the redesign replaced it with a full-bleed background treatment instead (see §9), so there's no fixed-size hero box left to migrate. Provider logos stay their own small brand-logo treatment — a different UI element than a poster, never in this token's scope. |
| CX2 | Rating display — five visual grammars, two colors, for the same number. | (1) single star + `/10`. (2) repeated-star string + `/10`. (3) single star, no `/10`. (4) plain text, no star. (5) interactive 10-star input. Color split evenly between `#ef9f27` and `#ba7517` with no rule for which gets which. | 🔴 High | One rating token (kill the second amber), one `<RatingBadge value size>` display component — always `★ N/10`, no repeated-star variant, no bare-star variant. Interactive input stays a separate component (genuinely different job: input vs. display). | ✅ fixed — [RatingBadge.jsx](src/components/ui/RatingBadge.jsx) used everywhere a rating is displayed; both old amber hexes and the repeated-star grammar are gone from the codebase (verified — zero remaining hits). [StarRating.jsx](src/components/StarRating.jsx) stays the separate interactive input, as the resolution specifies. |
| CX3 | Empty states — three different structures with no rule for which screen gets which. | Full (icon+title+subtitle+CTA): Library, Feed no-follows. Icon+title+subtitle, **no CTA**: Rankings, Stats, Diary — exactly where a nudge matters most. Single line, no icon: PublicListPage. Padding varies 2rem/3rem/4rem across otherwise-identical contexts. | 🔴 High | One `<EmptyState icon title subtitle action?>` component, structure always the same, action present everywhere there's an obvious next step (nearly everywhere — see GP6). | ✅ fixed — [EmptyState.jsx](src/components/ui/EmptyState.jsx) used consistently across Library, Feed, Rankings, Stats, Diary, Lists, PublicListPage. Rankings/Stats/Diary still don't all carry a CTA (GP6 tracks that specific gap separately — this row is about structural consistency, which is now real). |
| CX4 | ~110 hand-styled `<button>` elements resolve to roughly seven visual species with zero shared implementation — every instance a separate inline `style={{}}` object. | Pill-filled, pill-outline, rect-filled, rect-ghost, circular icon-only, text-link, one-off FAB. A global change (focus ring, disabled state, hover timing) currently means editing dozens of files by hand. | 🔴 High | `<Button variant size icon?>` covering all seven species, defined once. **Highest-leverage single extraction in the system** — most-repeated, most-inconsistent, most load-bearing for "looks designed on purpose." Full spec in `BINGR_DESIGN_SYSTEM.md`. | ✅ fixed — the actual harm this row names (944 inline `style={{}}` objects, no shared focus/disabled/hover behavior) is gone: 0 files in the app have inline styles left. [Button.jsx](src/components/ui/Button.jsx) covers the general-purpose cases everywhere. A handful of specialized multi-select controls (segmented status filters, season chips) intentionally keep their own small CSS-module treatment rather than forcing `<Button>`'s single-action model onto a toggle-group — a deliberate exception, not leftover inconsistency, since each still goes through tokens.css, not ad hoc values. |
| CX5 | 214 `borderRadius` declarations across 12 distinct values with no evident scale. | Values in use: 2, 3, 4, 5, 6, 8, 10, 12, 14, 16, 18, 20. 8 is most common (75×) but 10/12/20/6/16/14/4 are all heavily used too and aren't in any clean ratio to each other. Card padding equally ad hoc: 1/1.25/1.5/1.75/2/2.5rem. | 🔴 High | Collapse to 4 radii (sm/md/lg/pill) + an 8-step spacing scale — both defined in `BINGR_DESIGN_SYSTEM.md`. All 12 current values map onto the new scale; nothing new gets invented outside it. | ✅ fixed — every CSS Module in the app uses `tokens.css`'s 4 radii and 12-step spacing scale exclusively; with 0 files left on inline styles, there are no ad hoc pixel radius/padding values left anywhere to collapse. |
| CX6 | ~14 call sites format dates independently, each writing its own `toLocaleDateString('en-KE', {...})` options object. | At least 5 distinct format combinations for what's conceptually 3 needs (full date / short day+month / bare month). No shared utility. | 🟠 Medium | One `formatDate(date, 'full'|'short'|'month')` utility in `lib/`. Three named formats, every call site picks one. | ✅ fixed — [dates.js](src/lib/dates.js)'s `formatDate()` now used at every call site (13 across components/pages plus `lib/export.js` and `lib/stats.js`). Grew to 5 named formats instead of 3 — `lib/stats.js`'s chart-axis label and "busiest month" summary turned out to need bare month-only variants that would've broken layout if forced into the day/year-inclusive formats; noted here rather than silently expanding scope. |
| CX7 | Avatar (initials circle) — five sizes with no scale logic. | 32 (comment author), 34 (header trigger), 36 (feed item), 38 (FindPeople row), 64 (profile hero). Reads as five people independently guessing a number, not a deliberate scale. | 🟠 Medium | Three avatar sizes (sm=32, md=40, lg=64) covering every current context; retire the other four values. | ✅ fixed — [Avatar.jsx](src/components/ui/Avatar.jsx) primitive now used everywhere: header trigger, feed items, comment authors, FindPeople rows, profile hero |
| CX8 | The same three status colors (watched/watching/watchlist) get three different visual grammars depending on screen. | Solid-filled badge (MovieCard corner), colored *text* on neutral bg (LibraryTab progress line), colored *border/pill* (DetailPanel status buttons). | 🟠 Medium | One status treatment — solid-filled pill — everywhere `watched/watching/watchlist` is shown. Reads identically as badge, button, or inline chip. | ✅ fixed — [StatusPill.jsx](src/components/ui/StatusPill.jsx) used for MovieCard's badge and LibraryTab's "All" view row label (previously plain colored text). DetailPanel's status control is a *toggle* (needs an unselected state StatusPill doesn't model) so it stays a dedicated interactive component — but its active-state fill uses the exact same `--status-*` tokens, so the color language matches even though the container shape differs by necessity. |
| CX9 | Two competing modal paradigms with no rule for which context gets which. | Bottom sheet (slide-up): `LogEntryModal`, `FeedbackModal`, `SupportButton` panel, `CreateListModal`. Centered dialog: `OnboardingModal`. Nothing about onboarding's content demands the different treatment. | 🟡 Low | Standardize on the bottom sheet (more mobile-native, matches the stated mobile-heavy context) with a `size` prop for rare cases needing more vertical room. | ✅ fixed — every modal in the app (`LogEntryModal`, `FeedbackModal`, `CreateListModal`, `OnboardingModal`, plus the newer `ConfirmDialog`/add-to-list picker) now renders through the one [Modal.jsx](src/components/ui/Modal.jsx) primitive. `SupportButton` no longer exists as a modal at all — retired in Phase 2a into [SupportSection.jsx](src/components/SupportSection.jsx)'s plain page content. |
| CX10 | Destructive-action confirmation is inconsistent in *whether* it happens and jarring in *how* when it does. | `window.confirm()` (native, unstyled, breaks the visual language entirely) guards list/comment/diary-entry delete, library remove, admin promote/demote. Unfollow and "remove item from list" fire instantly with zero confirmation. | 🔴 High | One in-app `<ConfirmDialog>` (styled, on-brand) for anything destroying data that isn't trivially recreated (lists, diary notes, comments). Cheaply-reversible actions (unfollow, remove-from-list) stay instant but toast with a ~5s **Undo** — better UX than a prompt for low-stakes actions. | ✅ fixed and browser-verified — every remaining `window.confirm()` goes through `<ConfirmDialog>`. Unfollow ([UserProfilePage.jsx](src/pages/UserProfilePage.jsx), [FindPeople.jsx](src/components/FindPeople.jsx)) and remove-from-list ([ListsPage.jsx](src/pages/ListsPage.jsx)) are instant with a real toast **Undo**, confirmed working end-to-end (incl. a DB-level check) via Playwright MCP — which also caught a real bug the code-only pass missed: Undo was calling `toggleFollow()` again, but that closure was frozen with a stale `following` snapshot from before the unfollow, so it silently fired a second unfollow instead of restoring the follow. Fixed by having Undo call `follow()` directly. Library remove intentionally stays on `<ConfirmDialog>` rather than instant+undo — it wasn't one of this row's two named cheaply-reversible actions, and losing a library entry (ratings, watch status) isn't as trivially recoverable as a follow or a list membership. |
| CX11 | A global toast exists and is used for exactly two actions; everything else improvises its own feedback or gives none. | Toast covers: status change, rating change. Bespoke inline lookalikes: `ProfilePage`'s "✓ Profile saved," `SupportButton`'s "✓ Copied." Zero feedback: comment posted, list created, diary logged, follow toggled (see GP9), export downloaded. | 🟠 Medium | Route every success/error through the one toast system. Kill the bespoke inline confirmation boxes; a genuinely persistent (non-dismissing) state indicator is a different, deliberately-designed pattern, not an ad hoc toast lookalike. | ✅ fixed — new [ToastContext](src/contexts/ToastContext.jsx)/`useToast()` makes the toast reachable from anywhere, not just what App.jsx passes as props. `NavShell` was actually rendering a bare unstyled div for toast, not the real [Toast.jsx](src/components/ui/Toast.jsx) primitive built in Phase 0 — now wired correctly. Comment post, list create, list export, diary log, follow/unfollow, and data/library export all show a success toast. `ProfilePage`'s bespoke "✓ Profile saved" box replaced by a toast; its export-*error* box stays (persistent state, not a toast lookalike, per this row's own resolution text). |

---

## Table 3 — Gaps

*What's missing that the design implies should exist. Interaction/design gaps only — backend feature gaps are tracked in `BINGR_AUDIT_REPORT.md`.*

| ID | What's missing | Severity | Resolution | Status |
|---|---|---|---|---|
| GP1 | No way to see who you follow or who follows you. Profile pages show *counts* but they aren't tappable — no list view exists anywhere. You can grow the number but never audit, browse, or unfollow from it. | 🔴 High | Make follower/following counts tap targets opening a `<FollowerListSheet>`, reusing `<FollowButton>` (RD7) per row. | ✅ fixed — [FollowerListSheet.jsx](src/components/FollowerListSheet.jsx), counts on `UserProfilePage` now open it. New `useFollows.getUsers()` query — not yet verified against the live schema (guesses the FK-disambiguation embed syntax for `bingr_follows`'s two FKs into `profiles`); falls back to an empty list rather than crashing if the embed syntax is wrong. Worth a real check. |
| GP2 | Zero onboarding for a first-time user landing on an empty Discover. No welcome state, no explanation of what the tabs mean, no hint that a poster has quick actions — and on mobile those quick actions are hover-only and literally unreachable by touch, compounding the confusion. | 🔴 High | A lightweight, skippable first-session overlay (3–4 beats max) pointing at search, a poster's quick actions, and navigation. Given the growth-product ambition, this is the difference between a signup converting or bouncing after one confused scroll. | ✅ fixed — the touch-unreachability half was a real functional bug, not just missing orientation: [MovieCard.jsx](src/components/MovieCard.jsx)'s quick-actions overlay was pure CSS `:hover`, no touch equivalent. Now has an always-visible small tap toggle (`.actionsToggle`) that works identically on mouse and touch; desktop keeps the hover-reveal as a shortcut on top of it. New [WelcomeTour.jsx](src/components/WelcomeTour.jsx) (3-beat, skippable, via the existing `Modal` primitive — same shell as every other sheet, not a bespoke overlay) covers search / poster quick actions / the 5-tab nav, shown once per browser via a localStorage flag right after onboarding (`profile.username_set`) completes. |
| GP3 | Landing page has zero visual product preview — every section is text and emoji icons, no screenshot, no poster collage, no mockup. The marketing page for a movie-tracking app currently shows zero movie posters. | 🔴 High | Priority item in `BINGR_DESIGN_SYSTEM.md`'s landing redesign — hero needs real visual weight from the catalogue itself. | ✅ fixed (Phase 4) — [LandingPage.jsx](src/pages/LandingPage.jsx)'s poster-collage hero |
| GP4 | No search or filter within your own library. The Movie/TV filter is the only narrowing tool. With any real collection size, no way to jump straight to a specific tracked title. | 🟠 Medium | Lightweight client-side filter input at the top of the (post-RD2) Library view — data's already loaded, this is a `.filter()` away. | ✅ fixed (Phase 3) — [LibraryPage.jsx](src/pages/LibraryPage.jsx) filter input |
| GP5 | No sort control where you'd actually use it. `ExportPanel` has a sort-by dropdown (rating/title/year/added) that only affects the *exported file* — the on-screen list has no sort control at all. | 🟠 Medium | Move sort into the on-screen Library view's header controls; export inherits the currently-applied sort instead of maintaining a separate selector. | ✅ fixed (Phase 3) — sort now lives in [LibraryPage.jsx](src/pages/LibraryPage.jsx), [ExportPanel.jsx](src/components/ExportPanel.jsx) exports whatever's on screen instead of maintaining its own duplicate selectors |
| GP6 | About half the empty states describe a next action but don't let you take it — Rankings/Stats/Diary tell you what to do in text with no button to do it. (Duplicate of CX3's root cause, tracked separately because it's a distinct user-facing symptom.) | 🟠 Medium | Same fix as CX3 — every empty state gets a real CTA, no exceptions. | ✅ fixed — [RankedList.jsx](src/components/RankedList.jsx), [StatsPage.jsx](src/pages/StatsPage.jsx), and [DiaryPage.jsx](src/pages/DiaryPage.jsx)'s empty states now render a "Browse Discover" CTA via a `onGoDiscover` callback threaded down from `App.jsx`. `RankedList`'s read-only render on a *visited* profile (`UserProfilePage`) correctly gets no CTA — `onGoDiscover` is only passed on the owner's own Rankings tab. |
| GP7 | No edit path for an existing diary entry — `LogEntryModal` only creates; `DiaryPage` offers delete but not edit. A typo means delete-and-relog, losing the original entry's exact metadata. | 🟠 Medium | Make diary rows tappable into an edit variant of `LogEntryModal`, pre-filled. | ✅ fixed — [WatchLogCard.jsx](src/components/WatchLogCard.jsx)'s diary variant gets a pencil action next to delete; opens [LogEntryModal.jsx](src/components/LogEntryModal.jsx) pre-filled via a new `editEntry` prop, saving through `useDiary`'s `updateEntry` — which already existed in the hook but was never called from any screen until now. Editing doesn't re-sync the overall library rating (that's RD3's rating-sync logic, out of scope here). |
| GP8 | The two most-shared, most-public-facing pages have no back affordance beyond a clickable logo — `UserProfilePage` and `PublicListPage`, exactly where a cold visitor lands via a shared link. The logo-click is also a full page reload, not an in-app navigate, discarding a logged-in visitor's app state. | 🟠 Medium | Explicit, labeled back affordance consistent with the rest of the app; make the logo-click an in-app navigate. | ✅ fixed — both pages now have an explicit "← Back" button (`ArrowLeft` + label) alongside the logo, both wired to the same in-app `onGoHome` |
| GP9 | Two meaningful actions give literally zero feedback: follow/unfollow (only signal is the button's own state flip — no toast, unlike rating/status which do get one) and diary logging (modal just closes). | 🟠 Medium | Route both through the unified toast (CX11) — *"Following @user"* / *"Logged to your diary."* | ✅ fixed — `handleFollow` in both [UserProfilePage.jsx](src/pages/UserProfilePage.jsx) and [FindPeople.jsx](src/components/FindPeople.jsx) toasts "Following @user"/"Unfollowed @user"; `LogEntryModal`'s `onSave` in [DetailPanel.jsx](src/components/DetailPanel.jsx) toasts "Logged to diary"/"Rewatch logged". |
| GP10 | No notification surface of any kind — follows, comment activity, nothing surfaces. For a product with a follow graph and comments, the total absence of "X followed you" is a real gap in what makes the social layer feel alive rather than static. | 🟡 Low (design-scope now; underlying data mostly doesn't exist yet either — see `BINGR_AUDIT_REPORT.md`) | Out of scope for a pure UI pass, but the header/nav redesign should reserve visual space for a future notification indicator — cheaper to design in now than retrofit. | ✅ fixed (as scoped) — [Header.module.css](src/components/nav/Header.module.css) documents the reserved slot in `.rightGroup`. Deliberately not rendering a bell icon with no destination yet (no notification feature/data exists — a dead-end icon would be worse than no icon). The actual notification feature remains out of scope, as this row itself says. |
| GP11 | Own-profile privacy state is invisible outside Settings — nothing in the main app persistently reminds you your profile is set to Private. | 🟡 Low | A small lock/globe glyph next to the header avatar, matching whichever icon is assigned to the visibility toggle. | ✅ fixed — [Header.jsx](src/components/nav/Header.jsx) shows a small lock badge on the avatar when the profile is private; nothing shown for public (the default), to avoid always-on noise for most users. |
| GP12 | Sign-in has no visual identity of its own — `AuthPage`/`LandingPage` are both plain centered cards on a flat background, in a domain (film/TV) where poster art could establish mood before the user's even inside. | 🟡 Low | Priority item in `BINGR_DESIGN_SYSTEM.md` — auth should carry the same visual signature as the rest of the app, not read as a generic form. | ✅ fixed (Phase 4) — [AuthPage.jsx](src/pages/AuthPage.jsx)'s muted poster-collage strip |
| GP-nav | *(Cross-cutting, not a single row — the navigation redesign itself.)* The nine-tab horizontal scroll strip has no clear hierarchy, hides tabs 5–9 off-screen on mobile with no scroll affordance, and the floating ☕ support button sits in the same bottom-right thumb zone a bottom nav would need. | 🔴 High | Full navigation redesign in `BINGR_DESIGN_SYSTEM.md` — this is the single most consequential screen-level decision in Part B, not a per-row tweak. | ✅ fixed (Phase 2a+2b) — nine tabs collapsed to five (RD1/RD2/RD6 above), floating support button retired into the You hub, and the horizontal strip replaced with [BottomNav.jsx](src/components/nav/BottomNav.jsx) (mobile) / [SideRail.jsx](src/components/nav/SideRail.jsx) (desktop ≥1024px) per the design doc's nav spec. Avatar now opens the You hub's Account tab directly instead of a separate dropdown. |
| GP13 | Header logo never actually reached the true top-left corner on desktop (≥1024px) — `Header` was `position: sticky`, a normal-flow child of `.shell`, so `NavShell.module.css`'s `padding-left: 220px` (meant only to clear the `position: fixed` `SideRail`) shifted the logo 220px in too. `SideRail` occupied the real corner instead, empty. Mobile was unaffected (`SideRail` becomes a bottom bar there, no competing padding). Found via a `getBoundingClientRect` sweep (logo `x: 220`, not `0`) prompted by direct user feedback. | 🟠 Medium | Make `Header` `position: fixed`, full-width, above `SideRail`; give `SideRail` a `top: var(--header-height)` offset and `.shell` a matching `padding-top`. | ✅ fixed and browser-verified (both viewports) — [Header.module.css](src/components/nav/Header.module.css), [SideRail.module.css](src/components/nav/SideRail.module.css), [NavShell.module.css](src/components/nav/NavShell.module.css), new `--header-height` token in [tokens.css](src/styles/tokens.css). Confirmed via Playwright: logo `getBoundingClientRect().x` now `0` at desktop width, no gap/overlap between header and `SideRail`/page content, header stays fixed on scroll on both breakpoints. |
| GP14 | Stray vertical scrollbar/arrow artifact on the You-hub's in-page tab bar (`Stats \| Rankings \| Lists \| Support \| Account`), which never actually overflows vertically. Root cause: `YouHub.module.css`'s `.tabs` set `overflow-x: auto` without an explicit `overflow-y` — per the CSS spec, browsers compute the unset axis as `auto` too rather than leaving it a mismatched `visible`, so a real (if pointless) vertical scrollbar rendered. Worse-looking on mobile, where it drew as a full classic double-scrollbar. Found during a Playwright visual sweep, not user-reported. | 🟡 Low | Add `overflow-y: hidden` explicitly next to `overflow-x: auto`. | ✅ fixed and browser-verified (both viewports) — [YouHub.module.css](src/pages/YouHub.module.css). Confirmed via `elementFromPoint` at the tab bar's right edge hitting the tab-bar div itself, not a scrollbar track. |

---

## UI Review Checklist

Added after a round of user-reported issues turned out to share a few root-cause shapes. Future review passes (user-prompted or proactive) should check each screen against these categories instead of relying purely on eyeballing:

1. **Corner/edge positioning** — do brand/logo, primary nav, and page titles actually sit flush at the edges they visually imply (top-left, top-right), on both breakpoints? (Root cause of GP13.)
2. **Duplicate/competing elements** — same icon, label, or control rendered in two places doing the same job. (Root cause of the earlier duplicate-"bingr"-logo bug, fixed alongside the watchlist-independence work.)
3. **Click-target overlap** — do hover/overlay affordances block the click target underneath them? (Root cause of the poster-overlay-blocking-detail-click bug.)
4. **Mobile/desktop parity** — does a fix or feature verified on one breakpoint regress the other? (Root cause of the `AccountMenu` mobile-wrap regression, caught before shipping via direct mobile-viewport Playwright testing.)
5. **Overflow/scroll artifacts** — any scrollbar, anywhere in the app, that appears without content that actually overflows. (Root cause of GP14.)

## Root cause, stated once

Nearly every Contradiction traces back to one fact: **there is no shared component library.** 929 inline `style={{}}` objects means every button, card, avatar, and empty state was typed fresh by whoever touched that screen that day. The component specs in `BINGR_DESIGN_SYSTEM.md` aren't just a style guide — they're the structural fix for most of this document at once. Redundancies RD1–RD3 are the two I'd prioritize above pure visual work: they're not just inconsistent, the *current behavior is confusing on its own terms*, independent of any redesign.

## Tracker summary

| Table | Total rows | Fixed | Partial | Open |
|---|---|---|---|---|
| Redundancies | 10 | 8 (RD1, RD2, RD4, RD5, RD6, RD7, RD9, RD10) | 1 (RD8) | 1 (RD3) |
| Contradictions | 11 | 11 (CX1, CX2, CX3, CX4, CX5, CX6, CX7, CX8, CX9, CX10, CX11) | 0 | 0 |
| Gaps | 14 + 1 cross-cutting | 15 (GP-nav, GP1, GP2, GP3, GP4, GP5, GP6, GP7, GP8, GP9, GP10, GP11, GP12, GP13, GP14) | 0 | 0 |
| **Total** | **36** | **34** | **1** | **1** |
