# Dependencies
node_modules/

# Build output
dist/

# Environment variables — NEVER commit this
.env
.env.local
.env.production

# Editor
.DS_Store
.vscode/

# Playwright MCP session artifacts (screenshots, snapshots, console logs)
.playwright-mcp/

# Supabase
# NOTE: schema/migrations MUST be tracked — they are the source of truth for
# RLS policies and are reviewable alongside the code that depends on them.
# Only ignore local/generated state.
supabase/.branches/
supabase/.temp/
supabase/config.toml